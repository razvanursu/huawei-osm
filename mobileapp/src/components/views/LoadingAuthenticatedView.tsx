import React from "react";
import { ActivityIndicator } from "react-native";

const LoadingAuthenticatedView: React.FC = () => {
    return (
        <ActivityIndicator />
    )
}

export default LoadingAuthenticatedView